<ul>
    <% for (const item of items) { %>
        <% if (item.title) { %>
            <li><%= item.date %> <a href="<%- item.path %>"><%= item.title %></a> [<%= item.categories %>]</li>
        <% } %>
    <% } %>
</ul>